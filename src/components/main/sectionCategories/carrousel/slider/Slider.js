import React from 'react'
import SliderWrapper from './SliderWrapper';

export default function SliderCarousel({ index, data }) {

  return (
    <>
      <SliderWrapper index={index} data={data} />
    </>
  )
}